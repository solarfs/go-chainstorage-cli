name: GitHub Actions Build and Deploy
on:
  push:
    branches:
      - kangdev
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Build
      uses: solarfs/go-chainstorage-cli@kangdev

    - name: Build
      run: |
        pwd
        date
